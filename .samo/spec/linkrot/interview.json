{
  "slug": "linkrot",
  "persona": "Veteran \"CLI software engineer\" expert",
  "generated_at": "2026-04-20T17:42:22.158Z",
  "questions": [
    {
      "id": "input-source",
      "text": "What is the primary input format the CLI should accept for link discovery?",
      "options": [
        "Markdown files (recursive directory scan)",
        "Plain text file with one URL per line",
        "Sitemap.xml URL",
        "Stdin pipe of URLs",
        "All of the above (auto-detect)",
        "decide for me",
        "not sure — defer",
        "custom"
      ]
    },
    {
      "id": "check-semantics",
      "text": "What HTTP behavior counts as a 'broken' link?",
      "options": [
        "Only 4xx/5xx after following redirects",
        "4xx/5xx plus DNS/timeout failures",
        "Any non-2xx status (including 3xx redirect chains)",
        "4xx/5xx plus soft-404 heuristics on body content",
        "decide for me",
        "not sure — defer",
        "custom"
      ]
    },
    {
      "id": "concurrency-model",
      "text": "How should the checker handle concurrency and rate limiting?",
      "options": [
        "Fixed global worker pool (e.g. --concurrency=N)",
        "Per-host concurrency cap with global pool",
        "Sequential by default, opt-in parallelism",
        "Adaptive based on response latency",
        "decide for me",
        "not sure — defer",
        "custom"
      ]
    },
    {
      "id": "output-format",
      "text": "What output formats must v0.1 support for results?",
      "options": [
        "Human-readable TTY only",
        "TTY + JSON",
        "TTY + JSON + JUnit XML (for CI)",
        "TTY + JSON + SARIF",
        "decide for me",
        "not sure — defer",
        "custom"
      ]
    },
    {
      "id": "exit-and-ci",
      "text": "How should exit codes and CI integration behave?",
      "options": [
        "Non-zero exit on any broken link",
        "Configurable threshold (--fail-on=broken|warn|none)",
        "Always exit 0; surface results in output only",
        "Distinct codes per failure class (network vs 4xx vs 5xx)",
        "decide for me",
        "not sure — defer",
        "custom"
      ]
    }
  ],
  "answers": [
    {
      "id": "input-source",
      "choice": "Markdown files (recursive directory scan)"
    },
    {
      "id": "check-semantics",
      "choice": "Only 4xx/5xx after following redirects"
    },
    {
      "id": "concurrency-model",
      "choice": "Fixed global worker pool (e.g. --concurrency=N)"
    },
    {
      "id": "output-format",
      "choice": "Human-readable TTY only"
    },
    {
      "id": "exit-and-ci",
      "choice": "Non-zero exit on any broken link"
    }
  ]
}
